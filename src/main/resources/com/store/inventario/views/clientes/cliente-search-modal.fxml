<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="550.0" prefWidth="700.0" styleClass="root" stylesheets="@../../styles/style.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.store.inventario.controller.clientes.ClienteSearchModalController">

    <VBox spacing="16.0" style="-fx-padding: 20;" AnchorPane.bottomAnchor="0" AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0" AnchorPane.topAnchor="0">

        <VBox spacing="4">
            <Label styleClass="header-title" text="Seleccionar Cliente" style="-fx-font-size: 18px;"/>
            <Label styleClass="header-subtitle" text="Busque y seleccione el cliente para asociarlo a esta venta" />
        </VBox>

        <HBox alignment="CENTER_LEFT" spacing="10">
            <TextField fx:id="txtBuscar" onAction="#handleBuscar" promptText="Buscar por DNI, RUC o Nombre..." styleClass="search-field" HBox.hgrow="ALWAYS" />
            <Button onAction="#handleLimpiar" styleClass="btn-secondary" text="Limpiar" />
            <Button fx:id="btnBuscar" onAction="#handleBuscar" styleClass="btn-primary" text="Buscar" />
        </HBox>

        <TableView fx:id="tblClientes" styleClass="table-view" VBox.vgrow="ALWAYS" onMouseClicked="#handleTablaClicked">
            <columnResizePolicy>
                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
            </columnResizePolicy>
            <columns>
                <TableColumn fx:id="colCodigo" prefWidth="120.0" text="Código" />
                <TableColumn fx:id="colNombre" prefWidth="250.0" text="Nombre / Razón Social" />
                <TableColumn fx:id="colDni" prefWidth="100.0" text="DNI" />
                <TableColumn fx:id="colRuc" prefWidth="120.0" text="RUC" />
            </columns>
        </TableView>

        <HBox alignment="CENTER_LEFT" spacing="10">
            <Label fx:id="lblResumenPaginacion" maxWidth="Infinity" styleClass="pagination-summary" text="Cargando clientes..." HBox.hgrow="ALWAYS" />
            <HBox alignment="CENTER_RIGHT" spacing="6">
                <Button fx:id="btnAnterior" onAction="#handlePaginaAnterior" styleClass="btn-secondary" text="Anterior" />
                <Button fx:id="btnSiguiente" onAction="#handlePaginaSiguiente" styleClass="btn-secondary" text="Siguiente" />
            </HBox>
        </HBox>

        <Separator />

        <HBox alignment="CENTER_RIGHT" spacing="12.0">
            <Button fx:id="btnCancelar" onAction="#handleCancelar" styleClass="btn-secondary" text="Cancelar" />
            <Button fx:id="btnSeleccionar" onAction="#handleSeleccionar" styleClass="btn-primary" text="Seleccionar" />
        </HBox>

    </VBox>

</AnchorPane>
